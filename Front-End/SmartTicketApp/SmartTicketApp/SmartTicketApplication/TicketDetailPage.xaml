<?xml version="1.0" encoding="utf-8" ?>
<ContentPage Padding="20" Title="{Binding Estabelecimento}"
             xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="SmartTicketApplication.TicketDetailPage">
    <StackLayout>
        <Label Text="{Binding Servico}" HorizontalOptions="CenterAndExpand" HorizontalTextAlignment="Center" TextColor="Black" FontSize="Medium"/>
         <Label Text="{Binding Data}" HorizontalOptions="CenterAndExpand" HorizontalTextAlignment="Center" TextColor="Black" FontSize="Medium"/>
        <Label Text="{Binding Nr_Acesso}" HorizontalOptions="CenterAndExpand" HorizontalTextAlignment="Center" TextColor="Black" FontSize="Medium"/>
        <Label Text="{Binding Estado}" HorizontalOptions="CenterAndExpand" HorizontalTextAlignment="Center" TextColor="Black" FontSize="Medium"/>
        <Label Text="{Binding Tempo_Espera}" HorizontalOptions="CenterAndExpand" HorizontalTextAlignment="Center" TextColor="Black" FontSize="Medium"/>
        <Label Text="{Binding Tempo_atendimento}" HorizontalOptions="CenterAndExpand" HorizontalTextAlignment="Center" TextColor="Black" FontSize="Medium"/>
    </StackLayout>
</ContentPage>